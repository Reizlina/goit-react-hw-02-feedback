import Feedback from './modules/Feedback/Feedback';

import './shared/styles/style.css';

export const App = () => {
  return (
    <div className="container">
      <Feedback />
    </div>
  );
};

// good={good} netural={netural} bad={bad}
